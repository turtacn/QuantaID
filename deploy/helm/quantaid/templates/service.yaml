apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantaid.fullname" . }}
  labels:
    {{- include "quantaid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quantaid.selectorLabels" . | nindent 4 }}
